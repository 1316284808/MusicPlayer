<UserControl x:Class="MusicPlayer.Controls.SpectrumAnalyzerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:MusicPlayer.Converters"
             xmlns:behaviors="clr-namespace:MusicPlayer.Helper"
             mc:Ignorable="d"
             d:DesignHeight="150" d:DesignWidth="400">

    <UserControl.Resources>
        <converters:SpectrumHeightConverter x:Key="SpectrumHeightConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Border Width="auto" Height="150" HorizontalAlignment="Center" Margin="20,0,0,0" CornerRadius="8"
          Visibility="{Binding IsSpectrumVisible, Converter={StaticResource BoolToVis}}">
        <Grid Margin="10">
            <!-- 音频频谱标题 -->
            <TextBlock Text=" " FontSize="12" FontWeight="SemiBold" 
                     HorizontalAlignment="Center" VerticalAlignment="Top" Opacity="0.7"/>
            <!-- 频谱数据显示控件 -->
            <ItemsControl ItemsSource="{Binding SpectrumData}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <!-- 频谱条均匀分布在一行中 -->
                        <UniformGrid Rows="1"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- 单个频谱条 - 高度根据音频数据动态变化 -->
                        <Border Margin="0" VerticalAlignment="Bottom" Width="14" CornerRadius="7,7,0,0"
                          Background="#FF0078D7">
                            <Border.Height>
                                <!-- 使用频谱高度转换器将音频数据转换为可视化高度 -->
                                <Binding Path="." Converter="{StaticResource SpectrumHeightConverter}">
                                    <Binding.ConverterParameter>150</Binding.ConverterParameter>
                                </Binding>
                            </Border.Height>
                            <!-- 添加发光效果增强视觉体验 -->
                            <Border.Effect>
                                <DropShadowEffect Color="#FF0078D7"
                                BlurRadius="7"
                                ShadowDepth="0" 
                                Opacity="0.6"/>
                            </Border.Effect>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>