<UserControl x:Class="MusicPlayer.Controls.AlbumControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicPlayer.Controls"
             xmlns:Helper="clr-namespace:MusicPlayer.Helper"
             xmlns:converters="clr-namespace:MusicPlayer.Converters" 
             mc:Ignorable="d"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" 
             xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:SearchBoxWidthConverter x:Key="SearchBoxWidthConverter"/>
    </UserControl.Resources>

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">   
            <TextBlock Text="专辑列表" FontSize="24" FontWeight="Bold" Margin="10" /> 
        </StackPanel>
        <StackPanel Grid.Row="1" VerticalAlignment="Center"  HorizontalAlignment="Right" Orientation="Horizontal"> 
            <ui:Button   Width="auto" ToolTip="搜索" Margin="0,0,5,0" Background="Transparent" BorderThickness="0" Command="{Binding SearchButtonClickCommand}">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Helper:CustomIcon Kind="Search"  />
                    <Separator Margin="2" />
                    <TextBox x:Name="SearchTextBox"  
                   FontSize="14" FontWeight="Normal"  Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"   
                   HorizontalAlignment="Left" VerticalAlignment="Center"   
                   MaxLength="20"
                   Width="{Binding IsSearchExpanded, Converter={StaticResource SearchBoxWidthConverter}}"
                   Background="Transparent" BorderThickness="0">
                        <TextBox.Resources>
                            <Style TargetType="TextBox">
                                <Style.Triggers>
                                    <Trigger Property="IsFocused" Value="False">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Text" Value="{Binding SearchText, TargetNullValue='搜索专辑...'}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>
                </StackPanel>
            </ui:Button>
            <!--列表计数显示-->

            <StackPanel   Orientation="Horizontal" MinWidth="80" VerticalAlignment="Center" HorizontalAlignment="Right">
                <ui:TextBlock Text="共" FontSize="14" VerticalAlignment="Center"  Opacity="0.7"/>
                <ui:TextBlock Text="{Binding AlbumCount}" FontSize="14" VerticalAlignment="Center" Margin="2,0"/>
                <ui:TextBlock Text="张" FontSize="14" VerticalAlignment="Center" Opacity="0.7"/>
            </StackPanel>


        </StackPanel>


        <Grid Grid.Row="2"  >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <!--侧边索引-->
            <Border   Grid.Column="0" Background="Transparent" CornerRadius="8" Padding="2,5">
                <ListBox 
                     ItemsSource="{Binding IndexList}"
                     SelectedItem="{Binding CurrentIndex}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Hidden"
                     Padding="5"  
                     BorderThickness="0"
                     VerticalAlignment="Top"
                     HorizontalAlignment="Right">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" 
                                   FontSize="12" 
                                   FontWeight="Normal" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"
                                   Cursor="Hand"
                                   Foreground="Black"
                                   Margin="0,1,0,1">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListBoxItem}}" Value="True">
                                            <Setter Property="Foreground" Value="#E6FFFFFF" />
                                            <Setter Property="FontSize" Value="14"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground"  Value="#E6FFFFFF"/>
                                            <Setter Property="FontSize" Value="14"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
            </Border>
            
            <!-- 专辑列表 -->
           
            <ListBox  Grid.Column="1"
                 x:Name="AlbumListBox" HorizontalContentAlignment="Left"
                 ItemsSource="{Binding FilteredAlbums}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto" 
                 ScrollViewer.CanContentScroll="True"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                  Helper:AlbumAlbumArtBehavior.IsEnabled="True"
Helper:AlbumAlbumArtBehavior.ViewModel="{Binding }"
                 >
           
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <!-- 先使用标准WrapPanel进行测试 -->
                    <!--<WrapPanel Orientation="Horizontal"    HorizontalAlignment="Left" VerticalAlignment="Top" />-->
                        <vwp:VirtualizingWrapPanel  ItemSize="300, 140" 
            Orientation="Horizontal" 
            SpacingMode="Uniform" 
            IsGridLayoutEnabled="True" />

                    </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate >
                <DataTemplate >
                    <Border Background="#E6FFFFFF"
                            CornerRadius="8"
                           Margin="10"
                           Width="280"
                           Height="120" 
                           Cursor="Hand">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" 
                                          Command="{Binding DataContext.NavigateToAlbumDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                          CommandParameter="{Binding Name}"/>
                        </Border.InputBindings>
                           
                            <!-- 专辑卡片内容 -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/> 
                                </Grid.ColumnDefinitions>

                                <!-- 专辑封面  -->
                            <Border Grid.Column="0" Margin="0,0,2,0"
                                   Width="120"
                                   Height="120"
                                   CornerRadius="8"
                                    Background="#80FFFFFF" 
                                   HorizontalAlignment="Center" >
                                <Border.Effect>
                                    <DropShadowEffect Color="Black"
                                                     BlurRadius="8"
                                                     ShadowDepth="1"
                                                     Opacity="0.2" />
                                </Border.Effect>
                                
                                <!-- 专辑封面图片 -->
                                <Image Source="{Binding CoverImage}"   Margin="10"
                                      Stretch="UniformToFill">
                                    <!-- 如果没有封面图片，显示默认封面 -->
                                    <Image.Clip>
                                        <RectangleGeometry   RadiusX="8"   RadiusY="8" Rect="0,0,100,100" />
                                    </Image.Clip>
                                </Image>
                            </Border>
                                
                            
                            <StackPanel Grid.Column="1"  HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10"> 
                            <!-- 专辑名称 -->
                                <TextBlock  
                                    Text="{Binding Name}"
                                    FontSize="14"
                                    FontWeight="SemiBold"
                                    TextAlignment="Left"
                                    TextWrapping="Wrap" 
                                    LineHeight="20"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Left" 
                                    TextTrimming="CharacterEllipsis"
                                    ToolTip="{Binding Name}"
                                    MaxWidth="110" 
                                    Height="60" MaxHeight="80"/>   

                                <!-- 歌曲数量 -->
                            <TextBlock  
                                      Text="{Binding SongCount, StringFormat='{}{0} 首歌曲'}"
                                      FontSize="12"
                                      Foreground="Gray"
                                      TextAlignment="Left"
                                      Margin="0,8,0,0" /> 
                                </StackPanel>




                                <ui:Button  Grid.Column="0" Background="#80FFFFFF"
                                            HorizontalAlignment="Center"   VerticalAlignment="Center" 
                                            CornerRadius="15"
                                             Command="{Binding DataContext.PlayAlbumCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                             CommandParameter="{Binding Name}" 
                                            >
                                    <Helper:CustomIcon>
                                        <Helper:CustomIcon.Style>
                                            <Style TargetType="Helper:CustomIcon" BasedOn="{StaticResource {x:Type Helper:CustomIcon }}">
                                                <!-- 默认显示播放图标 -->
                                                <Setter Property="Kind" Value="Play"/>
                                                <Style.Triggers>
                                                    <!-- 播放时显示暂停图标 -->
                                                    <DataTrigger Binding="{Binding IsPlaying}" Value="True">
                                                        <Setter Property="Kind" Value="Pause"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Helper:CustomIcon.Style>
                                    </Helper:CustomIcon>
                                </ui:Button>
                                 
                                
                            </Grid>
                        
                        <!-- 鼠标悬停效果 -->
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#E8E8E8" />
                                            <Setter Property="BorderBrush" Value="#CCCCCC" />
                                            <Setter Property="BorderThickness" Value=" 1 " />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            
            <!-- 简化容器样式，移除默认的选中样式 -->
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
           
        </Grid>
    </Grid>
</UserControl>