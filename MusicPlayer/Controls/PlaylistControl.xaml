<UserControl x:Class="MusicPlayer.Controls.PlaylistControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:MusicPlayer.Converters"
             xmlns:Helper="clr-namespace:MusicPlayer.Helper"
             xmlns:config="clr-namespace:MusicPlayer.Config"
             mc:Ignorable="d" >



    <UserControl.Resources>
        <converters:NotNullToBooleanConverter x:Key="NotNullToBooleanConverter"/>
        <converters:SortRuleToSymbolConverter x:Key="SortRuleToSymbolConverter"/>
        <!-- 搜索框宽度转换器 -->
        <converters:SearchBoxWidthConverter x:Key="SearchBoxWidthConverter"/>
    </UserControl.Resources>

    <Grid Helper:PlaylistInitializationBehavior.IsEnabled="True"
          Helper:PlaylistInitializationBehavior.PlaylistViewModel="{Binding}"
          Helper:PlaylistAlbumArtBehavior.IsEnabled="True"
          Helper:PlaylistAlbumArtBehavior.PlaylistViewModel="{Binding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="AUTO"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding PlaylistTitle}" FontSize="24" FontWeight="Bold" 
      HorizontalAlignment="Center" VerticalAlignment="Center" 
      Margin="10"   />
        <!-- 播放列表内容区域 -->
        <Border x:Name="ContentBorder" Grid.Row="1" CornerRadius="0,8,8,0" Background="Transparent" 
             
                Margin="0,0,10,0"
                HorizontalAlignment="Stretch" >
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Color="Black" Opacity="0.1"/>
            </Border.Effect>

            <Grid Margin="0,0,0,10">
                <Grid.RowDefinitions> 
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="9*"/>
                </Grid.RowDefinitions>
                
                <Border Grid.Row="0" VerticalAlignment="Center" >
                    <Grid  Margin="5" Cursor="">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- 添加按钮 -->
                        <ui:Button Grid.Column="0"  Command="{Binding AddMusicCommand}" ToolTip="添加音乐文件"
                          Margin="0,0,5,0"
                          Background="Transparent"  BorderThickness="0" >
                            <Helper:CustomIcon Kind="Add"    />
                        </ui:Button>
  
                        <!-- 排序下拉按钮 -->

                        <ComboBox Grid.Column="1" 
                                Background="Transparent"
                                Margin="0,0,5,0" 
                                 BorderThickness="0" 
                                Width="40" Height="35"
                                ToolTip="{Binding SortRuleText}"
                                ItemsSource="{Binding SortOptions}"
                                SelectedItem="{Binding SelectedSortOption, Mode=TwoWay}"
                                Style="{StaticResource WpfUiComboBoxStyle}"
                                ItemTemplate="{StaticResource SortOptionItemTemplate}"
                                HorizontalContentAlignment="Left"
                                VerticalContentAlignment="Center"/>


                        <ui:Button Grid.Column="3" Width="auto" ToolTip="搜索" Margin="0,0,5,0" Background="Transparent" BorderThickness="0" Command="{Binding SearchButtonClickCommand}">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Helper:CustomIcon Kind="Search" Margin="0,0,15,0"/>
                                <TextBox x:Name="SearchTextBox" Width="{Binding IsSearchExpanded, Converter={StaticResource SearchBoxWidthConverter}}"  
                                         FontSize="14" FontWeight="Normal"  Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"   
                                         HorizontalAlignment="Left" VerticalAlignment="Center"   
                                         MaxLength="20"
                                         Background="Transparent" BorderThickness="0">
                                    <TextBox.Resources>
                                        <Style TargetType="TextBox">
                                            <Style.Triggers>
                                                <Trigger Property="IsFocused" Value="False">
                                                    <Setter Property="Background" Value="Transparent"/>
                                                    <Setter Property="Text" Value="{Binding SearchText, TargetNullValue='搜索歌曲...'}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Resources>
                                </TextBox>
                            </StackPanel>
                        </ui:Button>
                        <!--列表计数显示-->

                        <StackPanel Grid.Column="4" Orientation="Horizontal" MinWidth="80" VerticalAlignment="Center" HorizontalAlignment="Right">
                            <ui:TextBlock Text="共" FontSize="14" VerticalAlignment="Center"/>
                            <ui:TextBlock Text="{Binding SongCount}" FontSize="14" VerticalAlignment="Center" Opacity="0.7" Margin="2,0"/>
                            <ui:TextBlock Text="首" FontSize="14" VerticalAlignment="Center" Opacity="0.7"/>
                        </StackPanel>
                      
                    </Grid>
                </Border>
                <!-- 播放列表 - 优化虚拟化设置，减少内存占用 -->
                <ListBox x:Name="PlaylistListBox" Grid.Row="1"  Margin="10" ItemsSource="{Binding FilteredPlaylist}" SelectedItem="{Binding CurrentPlaylistItem, Mode=TwoWay}" 
                           Background="Transparent" BorderThickness="0" AlternationCount="2"  
                           ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                           ScrollViewer.VerticalScrollBarVisibility="Auto"
                           ScrollViewer.CanContentScroll="True"
                           VirtualizingStackPanel.IsVirtualizing="True"
                           VirtualizingStackPanel.VirtualizationMode="Recycling"
                           VirtualizingStackPanel.ScrollUnit="Pixel"
                           VirtualizingStackPanel.CacheLength="0,0"
                           VirtualizingStackPanel.CacheLengthUnit="Item"
                           Helper:PlaylistScrollBehavior.IsEnabled="True" 
                           SelectionMode="Single"
                           Helper:PlaylistInteractionBehavior.IsEnabled="True"
                           Helper:PlaylistScrollToCurrentSongBehavior.IsEnabled="True">
                    <ListBox.ContextMenu>
                        <ContextMenu>
                            <MenuItem   Header="加入收藏"   Command="{Binding ToggleSongHeartCommand}" CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                <MenuItem.Icon>
                                    <Helper:CustomIcon Kind="Heart"  Foreground="Red" />
                                </MenuItem.Icon>
                            </MenuItem>
                            <Separator Margin="2"/>
                            <MenuItem Header="删除此项"  Command="{Binding DeleteSelectedSongCommand}" CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"   >
                                <MenuItem.Icon>
                                    <Helper:CustomIcon Kind="Delete" Foreground="Red" />
                                </MenuItem.Icon>
                            </MenuItem>
                        </ContextMenu>
                    </ListBox.ContextMenu>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <!-- 优化虚拟化性能的设置 -->
                            <Setter Property="Padding" Value="5,10"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

                            <!-- 使用附加属性处理容器生命周期事件 -->
                            <Setter Property="Helper:PlaylistItemBehavior.IsEnabled" Value="True"/>
                            <Setter Property="Helper:PlaylistItemBehavior.ItemsControl" 
                                    Value="{Binding RelativeSource={RelativeSource AncestorType=ListBox}}"/>

                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="ItemBorder" CornerRadius="8" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>

                                        <ControlTemplate.Triggers>
                                            <!-- 容器可见性变化时触发相应的处理 -->
                                            <Trigger Property="IsVisible" Value="False">
                                                <Setter TargetName="ItemBorder" Property="Opacity" Value="0"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>

                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#e6e6e6"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="False">
                                    <Setter Property="Background" Value="Transparent"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#e6e6e6"/> 
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>
                               
                                <!-- 专辑封面缩略图 -->
                                <Border Grid.Column="0"  Width="40" Height="40" CornerRadius="8" Background="#1AFFFFFF" Margin="0,0,8,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <!-- 当专辑封面为空时显示默认图标 -->
                                                <DataTrigger Binding="{Binding AlbumArt}" Value="{x:Null}">
                                                    <Setter Property="Background" Value="#33FFFFFF"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <Grid>

                                        <Helper:CustomIcon Kind="MusicNote"   IconSize="20"  
                                            HorizontalAlignment="Center" VerticalAlignment="Center"
                                            Visibility="{Binding AlbumArt, Converter={StaticResource NotNullToBooleanConverter}, ConverterParameter=Invert}"/>

                                        <!-- 实际专辑封面 -->
                                        <Image Source="{Binding AlbumArt}" Stretch="Fill" 
                                 Visibility="{Binding AlbumArt, Converter={StaticResource NotNullToBooleanConverter}}">
                                            <Image.Clip>
                                                <RectangleGeometry   RadiusX="8"   RadiusY="8" Rect="0,0,39,39" />
                                            </Image.Clip>
                                        </Image>
                                    </Grid>
                                </Border>
 
                                <!-- 歌曲信息 -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold"     FontSize="14" Margin="0,0,0,2"/>
                                    <TextBlock Text="{Binding Artist}" FontSize="12"     Opacity="0.7"   />
                                </StackPanel> 
                                <!--歌曲时长，音质图标-->
                                <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,0,25,0">
                                    <ui:TextBlock Text="{Binding TotalTimeText}"   FontSize="12"  VerticalAlignment="Center"  />
                                    <Helper:ButtonControl AudioFormat="{Binding AudioFormat}"></Helper:ButtonControl>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <ui:Button Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="5" Width="40" Height="40" CornerRadius="20"   ToolTip="定位当前播放歌曲"
                          Command="{Binding ScrollToCurrentSongCommand}">
                    <Helper:CustomIcon  Kind="Position" ></Helper:CustomIcon> 
                </ui:Button>

            </Grid>
        </Border>
        </Grid>
   
</UserControl>