<UserControl x:Class="MusicPlayer.Helper.ButtonControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicPlayer.Helper"
             xmlns:converters="clr-namespace:MusicPlayer.Converters"
             mc:Ignorable="d" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <converters:AudioFormatToVisibilityConverter x:Key="AudioFormatToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <StackPanel Orientation="Horizontal">
            <!-- HiRes徽标 - 用于FLAC/APE/WAV格式 -->
            <ui:TextBlock x:Name="HiRes" VerticalAlignment="Center" 
                          FontSize="10" FontWeight="SemiBold" Foreground="#FFF6B322" 
                          Text="HiRes" Margin="0,0,5,0"
                          Visibility="{Binding AudioFormat, RelativeSource={RelativeSource AncestorType=UserControl}, 
                                            Converter={StaticResource AudioFormatToVisibilityConverter}, 
                                            ConverterParameter=HiRes}" />
            
            <!-- SQ徽标 - 用于MP3/AAC/M4A格式 -->
            <ui:TextBlock x:Name="SQ" VerticalAlignment="Center" 
                          FontSize="10" FontWeight="SemiBold" Foreground="#00BCD4" 
                          Text="SQ"
                          Visibility="{Binding AudioFormat, RelativeSource={RelativeSource AncestorType=UserControl}, 
                                            Converter={StaticResource AudioFormatToVisibilityConverter}, 
                                            ConverterParameter=SQ}" />
        </StackPanel>
    </Grid>
</UserControl>
