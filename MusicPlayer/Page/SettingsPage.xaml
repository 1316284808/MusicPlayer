<Page x:Class="MusicPlayer.Page.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MusicPlayer.Page"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:MusicPlayer.Controls" 
      xmlns:Helper="clr-namespace:MusicPlayer.Helper"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="SettingsPage">

    <Grid  >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <!-- 页面标题 -->
        <StackPanel HorizontalAlignment="Center">
                <ui:TextBlock   Margin="10,2" Text="设置"  FontWeight="Bold"  FontSize="24"   />

        </StackPanel>
        <!-- 内容区域 -->
        <TabControl Grid.Row="1"  VerticalContentAlignment="Top" BorderThickness="0" BorderBrush="Transparent"  Margin="10" Background="Transparent" >

            <TabItem BorderThickness="0" BorderBrush="Transparent"   >
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Helper:CustomIcon Kind="Settings" Margin="2" />
                        <Separator Margin="2" />
                        <TextBlock Text="外观设置" FontSize="14"    FontWeight="Bold"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer VerticalScrollBarVisibility="Auto"  HorizontalScrollBarVisibility="Disabled" Padding="20">
                    <controls:WindowSettingsControl x:Name="WindowSettingsControl"   />
                </ScrollViewer>
            </TabItem> 
            <TabItem     >
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Helper:CustomIcon Kind="Sound" Margin="2" />
                        <Separator Margin="2" />
                        <TextBlock Text="音频设置" FontSize="14"    FontWeight="Bold"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer VerticalScrollBarVisibility="Auto"  HorizontalScrollBarVisibility="Disabled" Padding="20">
                    <controls:SoundSettingsControl x:Name="SoundSettingsControl" DataContext="{Binding SoundSettingsViewModel}" />
                </ScrollViewer>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Helper:CustomIcon Kind="List" Margin="2" />
                        <Separator Margin="2" />
                        <TextBlock Text="数据管理" FontSize="14"    FontWeight="Bold"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer VerticalScrollBarVisibility="Auto"  HorizontalScrollBarVisibility="Disabled" Padding="20">
                    <controls:PlaylistSettingControl x:Name="PlaylistSettingControl"    />
                </ScrollViewer>
            </TabItem>
        </TabControl>
        <StackPanel  Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center"  >
            <!--<Button Content="应用设置" 
                Command="{Binding ApplySettingsCommand}"
                HorizontalAlignment="Center"
                Width="120" Height="40"
                Background="#33FFFFFF"   />-->
        </StackPanel>

    </Grid>
</Page>