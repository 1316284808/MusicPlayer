<Page x:Class="MusicPlayer.Page.PlaylistPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MusicPlayer.Page"
      xmlns:controls="clr-namespace:MusicPlayer.Controls"
      xmlns:converters="clr-namespace:MusicPlayer.Converters"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="PlaylistPage">
    <Grid>
        <Grid.RowDefinitions> 
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
       
        <!-- 播放列表 -->
        <controls:PlaylistControl x:Name="PlaylistControl" Grid.Row="1"/>  
    </Grid>
</Page>
